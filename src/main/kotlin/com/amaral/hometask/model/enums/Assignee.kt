package com.amaral.hometask.model

/**
 * CHILD1 / CHILD2 map to the configured names (e.g. Clara / Bernardo).
 * BOTH = joint task that both must do together.
 * UNASSIGNED = not yet assigned to anyone.
 */
enum class Assignee { CHILD1, CHILD2, BOTH, UNASSIGNED }
