[build]
builder = "NIXPACKS"
buildCommand = "gradle bootJar"

[deploy]
startCommand = "java -Dspring.profiles.active=prod -jar build/libs/casa-tarefas-backend-0.0.1-SNAPSHOT.jar"
healthcheckPath = "/api/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
