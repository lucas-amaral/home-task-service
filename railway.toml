[build]
builder = "NIXPACKS"
buildCommand = "./gradlew bootJar"

[deploy]
startCommand = "java -Dspring.profiles.active=prod -jar build/libs/home-task-service.jar"
healthcheckPath = "/api/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
